<!DOCTYPE html>
<html>
<head>
  <title>PowerOf10</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
  <div class="pull-right">
    <% if user_signed_in? %>
    <p>Hello <%= link_to current_user.name, account_path %> | <%= link_to('Logout', destroy_user_session_path) %></p>
    <% else %>
    <p><%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %></p>
    <% end %>
  </div>
  <div>
    <p><%= link_to '10 Power', root_path %> &nbsp; The power of 10 in action</p>
  </div>
</div>

<div class="container">
  <%= yield %>
</div>

</body>
</html>
